(** Typed OCaml bindings to the Tauri JavaScript API.

    This package provides zero-dependency access to Tauri platform capabilities
    via js_of_ocaml and Brr. It does NOT depend on any nopal UI packages. *)

module App = App
module Event = Event
module Window = Window
