(** Type-safe bidirectional routing for Nopal applications.

    {!module:Platform} defines the navigation abstraction that platform backends
    implement. {!module:Router} provides route parsing, navigation commands, and
    subscriptions that integrate with the MVU loop. *)

module Platform = Platform
module Router = Router
