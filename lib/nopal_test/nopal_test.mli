(** Nopal Test — pure structural test renderer.

    This package provides a test renderer for [Element.t] trees. It has zero
    platform dependencies and compiles on native OCaml. *)

module Test_renderer = Test_renderer
