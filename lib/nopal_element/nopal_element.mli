(** Nopal Element — pure UI description types.

    This package defines [Element.t], the type that view functions return. It
    has zero platform dependencies and compiles on native OCaml. *)

module Element = Element
